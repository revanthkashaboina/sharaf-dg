import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Dashboards',
}

export default async function discountRequest() {
  return (
            <div>Dashboards Component</div>
         )
}