import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Discount Request',
}

export default async function discountRequest() {
  return (
            <div>Discount Request Component</div>
         )
}