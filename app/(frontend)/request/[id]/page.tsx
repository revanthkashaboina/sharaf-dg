import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Request',
}

export default async function Requestdetails() {
  return (
            <div>Request details Component</div>
         )
}